# URL do backend (Railway em prod, localhost em dev)
BACKEND_URL=http://localhost:8080

# Token interno para autenticar com /interno/**
X_INTERNAL_TOKEN=change_me

# ID do projeto a simular (legado — em prod o motor descobre automaticamente)
PROJECT_ID=1
